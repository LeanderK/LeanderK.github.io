# LeanderK.github.io

Personal academic website built from the `academic-homepage` Jekyll template.

## Local preview

Run:

```bash
bundle install
bundle exec jekyll serve
```

Then open the local URL shown by Jekyll.
